<Page x:Class="JingHaiApp.Pages.JoinTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:JingHaiApp.Pages"
        mc:Ignorable="d" 
        d:DesignHeight="570" d:DesignWidth="920"
        Title="JoinTest">

    <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}">
        <StackPanel>
            <Grid x:Name="Container">
                <Label Content="入会审核（自助预审）" Foreground="Black" VerticalAlignment="Top" HorizontalAlignment="Left" Width="246" Height="40" Margin="20,10,0,0" FontSize="25"/>
            </Grid>
            <Grid x:Name="Choose" Margin="0,50,0,0">
            </Grid>
        </StackPanel>
    </Border>
</Page>
